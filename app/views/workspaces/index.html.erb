<h2>tab workspaces</h2>
<p>
  The time is now: <%= Time.now %>
</p>

<table>
  <thead>
    <tr>
      <th>Name</th>
    </tr>
  </thead>
  <tbody>
    <% @workspaces.each do |workspace| %>
      <tr>
        <td>
          <%= workspace.workspace_name %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<button type="button"> <%= link_to 'Add new workspace', new_workspace_path %> </button>